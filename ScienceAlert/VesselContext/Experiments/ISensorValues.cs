namespace ScienceAlert.VesselContext.Experiments
{
    //public interface ISensorValues
    //{
    //    ScienceExperiment Experiment { get; }

    //    float CollectionValue { get; }
    //    float TransmissionValue { get; }
    //    float LabDataValue { get; }

    //    bool IsOnboard { get; }     // is this sensor's experiment onboard the craft?
    //    bool IsAvailable { get; }   // assuming it's onboard, could we activate the experiment right now?
    //    bool IsRunnable { get; }    // Assuming the experiment is both onboard and available, then could we actually run it (available, undeployed ModuleScienceExperiment?)
    //}
}
