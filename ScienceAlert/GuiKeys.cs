namespace ScienceAlert
{
    public enum GuiKeys
    {
        ApplicationLauncherSpriteSheet, // Texture2D
        CloseButtonTexture, // Texture2D
        LockButtonTexture, // Texture2D
        UnlockButtonTexture, // Texture2D
        ResizeCursorTexture, // Texture2D
        RescaleCursorTexture, // Texture2D

        DishTexture, // Texture2D
        ClipboardTexture, // Texture2D
        SampleTexture, // Texture2D

        CompactSkin, // GUISkin (compact)
        PopupSkin, // GUISkin

        WindowTitleBarButtonStyle, // GUIStyle
        ExperimentAlertToggleStyle, // GUIStyle
    }
}
